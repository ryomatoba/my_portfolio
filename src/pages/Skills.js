// src/pages/Skills.js
import React from 'react';
// 必要であれば他のコンポーネントをインポート
// import SkillBar from '../components/ui/SkillBar';

const Skills = () => {
  return (
    <div className="container mx-auto py-12 px-4">
      <h1 className="text-4xl font-bold text-center mb-12">スキル</h1>
      {/* ここにスキルに関するコンテンツを追加 */}
      <p>React, JavaScript, HTML, CSS, TypeScript などのスキルがあります。</p>
      {/* 例: スキルバーなどを表示 */}
    </div>
  );
};

export default Skills; // ← デフォルトエクスポート